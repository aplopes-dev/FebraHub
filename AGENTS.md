# Workspace memory (AGENTS.md)
Contexto persistente e autoritativo deste workspace. Siga e mantenha atualizado (conciso, alto-sinal).

## 🐴 REGRA #1 — RODE TUDO DE DEV NA IdeaPad, NÃO NESTE WORKSPACE
Este workspace NÃO roda dev. **TUDO** — `pnpm install`, `tsc`, `nest build`, `next build`, ESLint, `prisma generate`/`migrate`, testes, e2e, rodar a app, scripts — roda na **Lenovo IdeaPad** (nosso "burro de carga"). Não builde/tsc/lint localmente aqui (lento, sem toolchain, polui o ambiente).
- **Acesso:** `sshpass -p 'root' ssh -p 2222 root@172.17.0.1`. Node22/pnpm10/tsc/psql prontos. ⚠️ **A IdeaPad TAMBÉM tem `/root/FebraHub` + containers `febrahub_*` (só p/ BUILD) — NÃO é o homolog. Deploy é SÓ na 66. Confira `hostname` antes (IdeaPad=`febracis-IdeaPad...`, homolog=`srv867020`).**
- **Fluxo:** `git pull` na IdeaPad → rodar build/tsc/lint/testes lá → trazer só o resultado (erros, exit code).
- **Instalar deps:** `pnpm install --no-frozen-lockfile` na RAIZ do monorepo (NUNCA `npm install` em apps/api — puxa fastify 5.12 e gera erros falsos em main.ts; pinado é 5.10.0). Ao adicionar dep, regenerar lockfile: `pnpm install --lockfile-only` (senão `--frozen-lockfile` do Dockerfile quebra).
- **Build:** api `cd apps/api && npx --no-install prisma generate && npx --no-install nest build`; web `cd apps/web && npx --no-install next build`.
- GOTCHA: `*/` dentro de comentário JSDoc fecha o bloco; `jsonSeguro()` mantém tipo Decimal no TS mas vira string em runtime (use `as unknown as` ao castar).
- Exceção: o deploy no HOMOLOG (66) builda a imagem Docker no próprio box da 66 — isso é o deploy, não o loop de dev.

## FebraHub — stack (pnpm monorepo)
- **`apps/api/`** — NestJS + Prisma (Postgres `febrahub`). Módulos em `apps/api/src/modules/`. Schema `prisma/schema.prisma`; migrations `prisma/migrations/0000000000NN_*`. Storage MinIO. Auth por sessão JWT (cookie `fh_acesso`, guard só valida o JWT, não DB). Permissões via `PerfilAcesso`/`PerfilSetor` + `modules/permissoes/catalogo.ts` + `perfis-padrao.ts`.
- **`apps/web/`** — Next.js App Router. Rotas `src/app/(app)/<hub>/` (autenticadas) e públicas na raiz (`/cardapio/[slug]`, `/painel/[slug]`, `/pedido/[id]`). Componentes `src/components/`, API clients `src/services/api/`, types `src/types/`, menu `src/lib/menu.ts`. Tailwind + CSS por feature em `src/app/*.css`.
- **`web/` (legacy Vite+Supabase) + `netlify.toml` + stray root `FilaLoja.tsx` FORAM REMOVIDOS** (lixo; deploy é Docker/VPS, não Netlify). O front é só `apps/web`. **`db/`** = SQL migrations (01..90) das views `vw_*`. Migrations Prisma pressupõem tabelas de negócio (`fato_*`/`dim_*`/`mv_*`/`stg_*`) já existentes (vêm do dump Supabase, NÃO das migrations).

## ⚠️ TOPOLOGIA prod vs homolog (fácil confundir)
- **HOMOLOG** = `febracis-hom.aplopes.com` → VPS **`31.97.166.66`** (`/root/FebraHub`, branch `homolog`, remote `github.com/aplopes-dev/FebraHub`). Containers `febrahub_web`:3260 / `febrahub_api`:3261. É AQUI que trabalhamos e deployamos. Nginx `sites-enabled/febracis-hom.*.conf` → 127.0.0.1:3260/3261. root pw `1952aplA++++`. gh logado como Mr-nascimento.
- **PROD (público real)** = `febracis.aplopes.com` → **OUTRA VPS `72.61.131.155`** (Ubuntu). Repo `/opt/febrahub/app`, env `/opt/febrahub/etl.env`, branch `fix/auth-hub-conversations-ui`, remote **`github.com/mkdevelop3r/FebraHub`** (FORK diferente!), DB desatualizado (~migration 20). Postgres pw `DSBJj386J2LGnngpOR7Ws0Dg22FAJoo`. Acesso via jump pela 66: `ssh root@72.61.131.155` (SEM senha, chave autorizada). Deploy em prod é ALTO RISCO — só com aprovação explícita + backup.
- Nginx da 66 tem conf órfã `febracis.aplopes.com.conf` → :3120 (vazio) — ignorar; o prod real é a 155.

## Deploy no homolog (66)
**USE O SCRIPT: `cd /root/FebraHub && ./deploy.sh`** (versionado no repo). Flags: `--api-only` (pula build do web, ~rápido), `--no-pull` (usa código presente), `--no-build` (só migrate+up). Ele faz: pré-cheque do `.env` → git pull → build local (api+web) → `prisma migrate deploy` (container efêmero) → `up -d` → healthcheck da api. Equivale ao passo-a-passo manual: `build api web` → `run --rm --no-deps --entrypoint sh api -c "npx prisma migrate deploy"` → `up -d api web`.
- **⚠️ DIR LIVE = `/root/FebraHub`** (TODOS os containers `febrahub_*` têm `working_dir=/root/FebraHub`). `/home/febracis/FebraHub` é clone VELHO/não usado (git com "dubious ownership") — NÃO deployar de lá.
- **`.env` OBRIGATÓRIO em `/root/FebraHub/.env`** (não versionado; compose interpola `${VAR}` dele; se faltar, a api sobe com env vazio e não valida). Hosts p/ os containers (rede `interna`): `DATABASE_URL=...@postgres:5432/...` e `MINIO_ENDPOINT=minio` (NÃO `localhost`). URLs públicas = `https://febracis-hom.aplopes.com`. Inclui os `STONE_*`.
- Imagens buildadas LOCALMENTE (não ghcr). Web build no box carregado leva ~20-30min ("Retrying 1/3" é normal). CMD do api = `node dist/main.js` (migrations rodam no passo `run`, cwd `/app/apps/api`).
NUNCA commitar: `supabase/*.dump`, `CREDENCIAIS_SEED.txt`, `*.bak.*`, `.env`.

## Como montar e2e / dados reais no homolog
- prod→homolog: `pg_dump -U febrahub -Fc` na 155 → relay via scp (jump 66) → backup do homolog → DROP/CREATE schema public → `pg_restore` (ignora ~111 erros de schemas extras, ok) → `migrate deploy` reaplica as faltantes. Parar api/web durante o restore. Backup do homolog em `/tmp/homolog_backup.dump` (66); dump de prod em `/tmp/prod_febrahub.dump`.
- e2e API standalone na IdeaPad: dump schema-only de prod → `migrate deploy` → `node dist/main.js` com env dummy (JWT 32+ chars, MinIO local); auth via JWT Bearer assinado com JWT_ACCESS_SECRET + `tipo:'acesso'`.

## LOJA FEBRACIS — módulo loja-pedidos (COMPLETO, homologado, deployado)
`apps/api/src/modules/loja-pedidos/` + `loja-produtos/` + `pdv/` + `financeiro/`. Migrations 30-38. Fluxo: cardápio→checkout(reserva estoque)→PIX/cartão→confirmar→fila→preparo→pronto→retirada. PDV/balcão na mesma fila com split de pagamento. Estoque LOJA/DEPOSITO (CHECK `loja_estoque_reservado_ok`: reservado<=saldo_fisico). Anti-oversell via advisory lock por produto. Financeiro: venda→`financeiro_lancamentos` origem=pdv + rateio. Auditoria (`loja_auditoria`). Crons (@Interval): expiração de reserva + lembrete de pronto. WhatsApp proativo best-effort. SSE público `loja-pedidos/publico/eventos` + polling fallback.
- **Pagamentos**: arquitetura `PaymentProvider` (`pagamentos/`). AsaasProvider (PIX QR + cartão tokenizado). ManualProvider (fallback dev). Webhook `POST loja-pedidos/publico/webhook/asaas` (header `asaas-access-token`==`ASAAS_WEBHOOK_TOKEN`, idempotente). Envs: `ASAAS_API_KEY`, `ASAAS_BASE_URL`, `ASAAS_WEBHOOK_TOKEN`, `LOJA_RESERVA_EXPIRA_MIN`, `LOJA_LEMBRETE_PRONTO_MIN` (falta configurar em prod). Stone/Pagar.me NÃO implementado (arquitetura pronta).
- **Web**: telas `(app)/loja/{dashboard,fila,balcao,operacoes,auditoria,produtos,cardapio,tv}` + públicas. QR do cardápio (`operacoes/:slug/qrcode` + QrCardapioModal). Permissões `loja.pedidos.ver|operar|gerenciar`.
- **PDV oficial = /loja/balcao**. O antigo grupo de menu "PDV" (`/pdv`, ResumoPdv) foi REMOVIDO do menu (páginas /pdv/* seguem no disco). Saíram do sidebar por redundância (rotas seguem no disco p/ deep-link): Loja "Abrir cardápio"/"Painel/TV" (já em `OperacoesLoja`) e Estoque "Verificações pendentes"/"Recebimentos" (cópias do grupo Compras). Regra: não duplicar item que já é atalho dentro de outra tela nem espelhar href de grupo vizinho. **Telas afins fundidas em ABAS (`?aba=`, rota antiga vira `redirect()`):** Pedagógico "Represados" → aba de "Customer Success" (`/pedagogico/cs?aba=represados`); Loja "Metas mensais/curso"+"Fechamento" → abas de "Planejamento e metas" (`/loja/planejamento`). Balcão gera `LojaPedido` (não `PdvVenda`). Shell tela-cheia: `Shell.tsx paginaCheia = caminho.startsWith("/loja/balcao")` → oculta `fh-page-topo`, usa `.fh-main-cheia`. Para outra rota tela-cheia, incluir em `paginaCheia`.
- **BalcaoPdv.tsx** + `app/balcao.css`: layout caixa (busca+scanner, chips categoria, grid c/ badge estoque, carrinho, split, atalhos F#). `types/pdv.ts PdvProduto` estendido (categoria/imagemUrl/precisaPreparacao/controlaEstoque).

## Estoque DEPÓSITO — apostilas/materiais (planilha Salvador)
218 produtos DEPÓSITO importados (categorias `Apostilas` 37 + `Materiais (Depósito)` 181). São `LojaProduto` com **vendePdv=false, exibeCardapio=false, controlaEstoque=true, preco=0** → NÃO aparecem no PDV/Cardápio. Saldo só em `loja_estoque_saldos local=DEPOSITO` (LOJA=0). NÃO misturar com produtos da LOJA. Script idempotente: `apps/api/scripts/importar-estoque-deposito.mjs` + `estoque-deposito-apostilas.json` (planilha-fonte em `docs/referencias/controle-estoque-salvador-apostilas.xlsx`). Rodar no container: copiar p/ `node_modules/.import-dep.mjs` (único dir writable; ESM resolve @prisma/client de lá) e `node node_modules/.import-dep.mjs <json> [--commit]` (cwd `/app/apps/api`).

## Módulo Fornecedores (Compras) — DEPLOYADO homolog
`apps/api/src/modules/fornecedores/` (CRUD, situação, histórico, picker) sob `compras.ver`/`compras.operar`. Models `Fornecedor`+`FornecedorContato` (migration 34). `compra_cotacoes`/`compra_pedidos` ganharam `fornecedor_id` opcional (SET NULL). Web `/compras/fornecedores` + seletor no form de cotação (DetalheCompra.tsx).

## Imagens de produtos da Loja (homolog) — MinIO
8 produtos-demo vendáveis (SKUs `DEMO-*`, idempotente). Imagens no MinIO bucket `febrahub` prefixo `loja/produtos/*.jpg`. GOTCHA: bucket é private; prefixo `loja/` foi tornado leitura anônima: `docker exec febrahub_minio mc anonymous set download local/febrahub/loja`. URLs públicas: `https://febracis-hom.aplopes.com/febrahub/loja/produtos/<f>.jpg`. `mc alias set local http://127.0.0.1:9000 <ACCESS_KEY> <SECRET_KEY>` (creds em `docker exec febrahub_api printenv | grep MINIO`). Operação ativa: `CIS Externo — Ago/2026` (slug cis-externo).

## Módulo FISCAL — NFC-e (mod 65) + cupom não fiscal — DEPLOYADO homolog
Portado de `docs/references/citybox/services/fiscal-api` para DENTRO da `apps/api` (não microserviço). Doc: `apps/api/FISCAL.md`. Emite a partir de `PdvVenda` (fluxo /pdv/caixa); Balcão (LojaPedido) exigiria adaptar o emitter (pendente). BA delega NFC-e ao SVRS.
- **Backend** `apps/api/src/modules/fiscal/`: cripto.ts (AES-256-GCM, env `FISCAL_CERT_ENCRYPTION_KEY`), certificado.ts (pkcs12 node-forge), fiscal-config.service.ts (singleton id=1, cert A1→MinIO cifrado, CSC), comprovante.ts (HTML bobina 80mm/A4), fiscal.service.ts (não fiscal), fiscal-nfce.service.ts (venda→XML mod65→assina→QR/CSC→XSD→SVRS→protocolo, numeração sob advisory lock + idempotência), fiscal.controller.ts (`/fiscal/*`), nfce/ (xml-builder, signer xml-crypto, qr-code, soap mTLS, xsd-validator libxmljs2).
- **Schema**: 5 models (FiscalConfig, FiscalCertificado, FiscalSequencia, FiscalDocumento, FiscalEvento) + migration `39_fiscal`. Perms `fiscal.emitir`/`fiscal.gerenciar`.
- **Deps novas**: node-forge, xml-crypto, xmlbuilder2, `libxmljs2` (NATIVO), @types/node-forge.
- **GOTCHA CRÍTICO libxmljs2 no Docker (Alpine/Node22)**: prebuild NÃO cobre node-v127/musl e `pnpm rebuild` vira NO-OP → boot morre no xsd-validator. Solução no `apps/api/Dockerfile` (estágio prod-deps): `RUN LIB="$(find /app/node_modules/.pnpm -type f -name binding.gyp -path '*libxmljs2*' | head -1 | xargs -r dirname)" && cd "$LIB" && npx node-gyp rebuild` (o pacote fica profundo em .pnpm/…; toolchain python3/make/g++ já no estágio; ~147s). Dockerfile também COPIA `resources/` (xsd/wsdl/ca) p/ runtime.
- **Resources** `apps/api/resources/{xsd/nfe,wsdl/nfe,ca}`: **`ca/icp-brasil.pem` NÃO existe** (só README) — precisa antes de transmitir de verdade (senão TLS UNABLE_TO_GET_ISSUER_CERT). Resolvidos via `process.cwd()` = /app/apps/api.
- **Frontend**: `services/api/fiscal.ts`, `components/fiscal/PainelFiscal.tsx`, página `(app)/configuracoes/fiscal/page.tsx`, item no menu.ts. Botões "Comprovante" e "Cupom fiscal" em `components/pdv/VendasPdv.tsx`.
- **Pendências p/ produção**: `FISCAL_CERT_ENCRYPTION_KEY` no env; cert A1 real + credenciamento NFC-e + CSC (Vitor/contador); URLs `NFCE_QRCODE_URL_BA_HOMOLOGATION`/`NFCE_CHAVE_URL_BA_HOMOLOGATION` (sem default, placeholders no FISCAL.md a confirmar; urlChave ≤85 chars); mapear NCM/CFOP por produto (hoje default 00000000/5102, CST 00/CSOSN 102); ligar cancelamento (infra pronta, falta wire). Sem config, cupom NÃO fiscal já funciona; NFC-e mostra pendências na tela.

## Busca Global (Spotlight) — `Ctrl+K`/`⌘K`
Modal spotlight + botão "Buscar…" no header. `apps/web/src/lib/acoes-catalogo.ts` (catálogo de ações tipadas por módulo; `buscarAcoes(q, ctx)` normaliza NFD; **nova função → adicionar aqui no grupo** e aparece na busca). `components/shell/BuscaGlobal.tsx` (busca menus + ações, dedup por href, `useBuscaGlobal()` hook + `BotaoBuscaGlobal`). `app/busca-global.css` importado em layout.tsx. Shell.tsx renderiza os dois.

## Telas públicas redesenhadas
- **Cardápio** (`/cardapio/[slug]`, `components/loja/CardapioPublico.tsx` + `app/cardapio.css` escopo `.cdp`): storefront responsivo, tema ESCURO fixo (de propósito — vitrine igual em qualquer aparelho). Hero, nav de categorias com scroll-spy, grid 1→2→3 col, carrinho sidebar (desktop)/barra flutuante (mobile), checkout/PIX em sheet. Lógica de checkout/pagamento preservada.
- **Painel/TV** (`/painel/[slug]`, `components/loja/PainelTv.tsx` + `app/painel.css` escopo `.tv`): estilo QSR (McDonald's), tema escuro fixo, `clamp()`/vw p/ 1080p→4K. Duas colunas: `.tv-col.preparando` | `.tv-col.pronto` (verde). Relógio ao vivo; senha recém-pronta ganha `.novo` (pulsa 20s). Dados via SSE `useLojaPedidosStream` + polling 4s. (Bloco `.tv-*` em fila.css = dead code.)
- GOTCHA preview: browser das tools é remoto (proxy) — não alcança `127.0.0.1`; confie em tsc/lint.

## UI — padronização de botão dourado (contraste no tema claro) ⚠️ REGRA CORRETA
`--gold` MUDA por tema: no ESCURO é dourado-claro (#E4C06A), no CLARO é MARROM ESCURO (#8A6A1E). Por isso **NÃO usar `var(--gold)` como FUNDO de botão** — no claro fica marrom-escuro e o texto escuro `--sobre-ouro` (#100c04) por cima fica ILEGÍVEL (~2:1). `--gold` só serve p/ TEXTO/ícone/borda.
**REGRA:** o FUNDO do botão dourado é o gradiente **`linear-gradient(180deg,var(--gold-top),var(--gold-base))`** (#F2D488→#B8934A) — esses tokens são FIXOS e claros nos 2 temas (identidade do produto) e dão alto contraste com texto `var(--sobre-ouro)` no claro E no escuro. Tints translúcidos `rgb(var(--gold-rgb)/.xx)` levam texto `var(--gold)` (é fundo de página, ok).
- Helper: **`.fh-btn-ouro`** em `globals.css` (fundo = gradiente gold-top→base + color/svg sobre-ouro). Todos os `.ouro`/badges (`.loja-btn.ouro`, `.pdv-btn.ouro`, `.fin-btn.ouro`, `.bal-*`, `.co-*`, `.es-*`, chips `.ativo`, badges TSX) usam esse gradiente, NÃO `var(--gold)` de fundo.
- Tokens em `lib/tema.ts`: `C.gold`, `SOBRE_OURO`, `C.goldTop/goldBase`. Tema em `globals.css`: `:root` claro / `:root[data-tema="escuro"]` / `@media(prefers-color-scheme:dark)`. Cada cor publica `--x` e `--x-rgb`. `alfa()` monta `rgb(var(--x-rgb)/α)`.

## Loja — upload de imagem de produto (homolog, deployado via push)
No modal "Editar/Novo produto" (`apps/web/src/components/loja/CatalogoLoja.tsx` → componente `UploaderImagem`) o operador envia a foto direto pra API. No PDV móvel (`ModalEditarProdutoPdvMovel` em `pdv-movel/vender/page.tsx`) a imagem é comprimida no navegador (`comprimirImagem`, canvas ~1400px/JPEG) antes de subir. (A remoção de fundo automática via `@imgly/background-removal` FOI REMOVIDA — não reintroduzir.)
- **API**: `POST /loja/produtos/imagem` (multipart `arquivo`, perm `loja.produtos.gerenciar`) em `loja-produtos.controller.ts`/`.service.ts` (`enviarImagem`). Valida PNG/JPG/WEBP, sobe pro MinIO prefixo público `loja/produtos/` sob UUID, devolve `{ url }` público que vai em `imagemUrl`.
- **StorageService** ganhou `urlObjetoPublico(chave)` (`${MINIO_PUBLIC_URL}/${bucket}/${chave}`, encodeURIComponent por segmento; null se sem MINIO_PUBLIC_URL) e `garantirPrefixoPublico(prefixo)` (idempotente: lê/edita bucket policy p/ `s3:GetObject` anônimo). Ambos chamados no upload.
- Client fn `lojaEnviarImagemProduto(blob, nome)` usa `api.enviarArquivo`. CSS `.loja-uploader*` em `app/loja.css`. GOTCHA: `MINIO_PUBLIC_URL` na 66 = SÓ `https://febracis-hom.aplopes.com` (SEM `/febrahub`) — `urlObjetoPublico` já concatena `/${bucket}/`; com bucket na base o path duplica (`/febrahub/febrahub/…`→403).

## 🔁 REGRA CRUD COMPLETO — obrigatória em todo módulo/feature
**Sempre que implementar qualquer entidade ou feature, garantir CRUD completo antes de considerar pronto:**
- **C** — Create: endpoint `POST`, DTO de criação, validação, permissão `*.operar`, botão/modal "Novo" na UI.
- **R** — Read (list + detail): `GET /` (lista paginada + filtros) + `GET /:id` (detalhe), tela de listagem + tela/modal de detalhe na UI.
- **U** — Update: `PATCH /:id` (parcial) ou `PUT /:id`, DTO de atualização, botão "Editar" na UI (mesmo modal de criação ou tela própria).
- **D** — Delete (ou arquivar/inativar): `DELETE /:id` ou patch de `situacao/ativo`, confirmação na UI, soft-delete preferido (campo `deletedAt` ou `situacao`).
- **Checklist extra:** migration + schema Prisma, permissões no catálogo (`catalogo.ts` + `perfis-padrao.ts`), item no menu.ts (se for seção nova), ação no `acoes-catalogo.ts`, client em `services/api/`.
- **NÃO entregar** nenhum módulo sem todos os 4 pontos. Se o tempo/escopo não permitir, deixar TODO explícito no código e no AGENTS.md.

## Módulo Comercial (implementado)
Client `services/api/comercial.ts` (`/comercial/*`), CSS `app/comercial.css` (`.com-*`), telas `(app)/comercial/{page,pipeline,oportunidades/[id],vendas,leads}`. Menu grupo `comercial-hub` (menu.ts, antes de financeiro-erp); ações em `acoes-catalogo.ts` (`AcaoGrupo`+`acoes_comercial`); `import comercial.css` no `(app)/layout.tsx`. Perms `comercial.{ver,operar,gerenciar,vendas.aprovar,relatorios}`. Padrões: `api.*` (cookie httpOnly), `GuardaPermissao` por página, TanStack Query, tokens de tema (sem hardcode), centavos `(v/100).toLocaleString`, `use(params)` App Router.

## Módulo PEDAGÓGICO — telas web (homolog, deployado via push homolog)
Backend `apps/api/src/modules/pedagogico/` (controller v2 `pedagogico-novo.controller.ts`, base `/pedagogico/v2`, `@ExigeSetor('pedagogico')`); client `apps/web/src/services/api/pedagogico.ts` (obj `pedagogico.*`). Telas em `apps/web/src/app/(app)/pedagogico/*` usam CSS único `app/pedagogico.css` (escopo `.ped-*`) — **todas as classes usadas estão definidas lá** (tabela=`.ped-tabela`/`.ped-tabela-wrapper`, form=`.ped-form-card`/`.ped-form-grid`/`.ped-label`/`.ped-input`/`.ped-select`/`.ped-textarea`, botões `.ped-btn-primario|outline|xs`, abas `.ped-aba-*`, KPIs, banners, timeline). Sem GuardaPermissao no front (confia no menu + setor no back).
- **Telas criadas** (antes davam 404 — pastas sem page.tsx): represados, transferencias, monitores, solicitacoes, secretaria (jornada 360° via `/alunos/:pessoaId/jornada`), cs, presenca. + já existiam: dashboard, turmas (lista/novo/[id]), alunos, credenciamento, cadastros/avaliacoes*.
- **CRUD completo** dentro do que o back expõe: turma [id] tem **Editar** (modal, `atualizarTurma`/PUT) + mudar status; alunos tem **criar matrícula manual** (`criarMatricula`); CS tem **edição completa** (status/prioridade/próxima ação/prazo/obs/resultado via `atualizarCs`); transferencias tem **solicitar** (`solicitarTransferencia`) + efetivar/cancelar (acha a transf pendente via jornada, pois NÃO há GET list de transferências).
- **DELETE (soft-delete, sem migration — reusa status existentes)**: `DELETE /turmas/:id` (status→'Cancelada', BLOQUEIA se houver matrícula ativa notIn Cancelado/Transferido), `DELETE /matriculas/:id` (status→'Cancelado', preserva histórico, aceita body `{motivo}`), `DELETE /monitores/:id` (status→'inativo'), `DELETE /monitores/escala/:id` (HARD delete do vínculo), `DELETE /solicitacoes/:id` (HARD delete — registro leve), `DELETE /cs/:id` (status→'descartado'). Todos idempotentes. Client `pedagogico.remover*`. Botões na UI (Cancelar turma / Cancelar matrícula / Inativar monitor / remover escala / Excluir solicitação / Descartar CS). `api.delete(url, { corpo })` suporta body.
- **GOTCHAS**: (1) Status de turma no `@IsIn` exige strings COM espaço/acento — `'Aguardando Validação'`, `'Em Preparação'`, `'Em Andamento'` (o form antigo mandava `EmPreparacao` etc. e o back rejeitava). (2) Filtros de matrícula/turma usam `busca` e `porPagina` (DTO whitelist) — `q`/`limite` são ignorados silenciosamente (bug pré-existente já corrigido em alunos). (3) `<input type=date>` precisa `YYYY-MM-DD` → `String(iso).slice(0,10)`.
- Depende de dados nas tabelas `pedagogico_*` (do dump); no homolog telas podem aparecer vazias se não houver seed pedagógico — é o comportamento correto, não 404.

## LOJA — Retirada por QR (comprovante do cliente + resgate pelo vendedor) — validado na IdeaPad, deployado via push homolog
Fecha o loop do cardápio digital: após PAGAR, o cliente recebe um **comprovante** com **QR único**; o vendedor no balcão **escaneia** para verificar a compra paga, ver itens e **resgatar a retirada** com segurança. Migration `44_loja_retirada_qrcode` (aditiva, idempotente) + schema `LojaPedido`: `tokenRetirada` (String? @unique, base64url 256 bits), `retiradoPorId`/`retiradoPorNome`.
- **Backend** `loja-pedidos.service.ts`: `gerarTokenRetirada()` (randomBytes(32).base64url). Token gerado na **confirmação do pagamento** do cardápio (`confirmarPagamento`) e no **envio à preparação** do PDV (`vendaPdv` modo ENVIAR_PREPARACAO; ENTREGAR_AGORA já nasce RETIRADO → sem token). Métodos: `comprovante(id, origem)` (público, receita + QR PNG/SVG; sem token se não-pago/cancelado), `consultarRetirada(token)` (só leitura, veredito `podeRetirar` + `bloqueio` legível), `resgatarRetirada(token, u)` (transacional, trava por pedido, idempotente — 409 em QR reapresentado; grava quem resgatou; audita `pedido.retirado via:qrcode`). QR aponta p/ `${basePublica}/loja/retirada/<token>` (deep-link).
- **Controller**: `@Publica GET publico/pedidos/:id/comprovante`; `GET retirada/:token` + `POST retirada/:token/confirmar` (ambos `@ExigePermissao('loja.pedidos.operar')`). Sem permissão nova — reusa `loja.pedidos.operar` (texto do catálogo atualizado).
- **Web cliente**: `AcompanharPedido.tsx` (rota pública `/pedido/[id]`) agora mostra o comprovante com QR grande + itens + total + botão imprimir. CSS `app/comprovante.css` (escopo `.cmp-*`, tema escuro fixo + `@media print`).
- **Web vendedor**: tela `/loja/retirada` (+ `/loja/retirada/[token]` deep-link) → `RetiradaLoja.tsx` + `LeitorQr.tsx` (câmera via **html5-qrcode** por `import()` dinâmico, só no cliente; fallback "Digitar código" cola URL/token — `extrairToken()` aceita URL completa ou token puro). Card de verificação (pago/já-retirado/pode-retirar) + "Confirmar retirada" verde. CSS `app/retirada.css` (`.ret-*`, tema por tokens). Client `consultarRetirada`/`resgatarRetirada`/`comprovantePedido` em `services/api/loja-pedidos.ts`; types `Comprovante`/`RetiradaConsulta`.
- **Dep nova**: `html5-qrcode@^2.3.8` em `apps/web` (lockfile regenerado). Menu `loja-retirada` (após balcão, `loja.pedidos.operar`) + ação spotlight `loja-retirada` (ícone `QrCode` — registrado em `BuscaGlobal.tsx` ICONES) + atalho `<Link>` "Escanear retirada" no hero da FilaLoja.
- GOTCHA next lint: link interno entre páginas Next exige `next/link` `<Link>` (não `<a>`), senão `no-html-link-for-pages` quebra o build.

## Integração Stone (maquininha/PDV)
`StoneProvider` (Pagar.me v5) commitado, NÃO deployado. API CONNECT 2.0: `poi_payment_settings` em Pagar.me v5, confirma via webhook `charge.paid`. SEM sandbox real (SUNMI P2A11/T8 temporariamente fora da lista). Conciliação D+1: auth `x-user-type: client` OBRIGATÓRIO. ✅ Deployado no homolog (migration 46).

## Módulo OMIE — Integração Loja ↔ Omie (deployado homolog, migration 53)
`apps/api/src/modules/omie/` (controller `/loja/omie/*`). Perms `loja.pedidos.gerenciar`/`ver` + `loja.omie.gerenciar`. Schema: `OmieConfig`(id='omie'), `OmieLancamento`(pedidoId @unique), `LojaProduto.skuOmie`(String? @unique). SKU Sync: busca por `codigo` no Omie→`skuOmie`; cria produto se não acha (SKU `FH-00001` seq). Lançar = Pedido de Venda Omie. Web: `/loja/vendas` (VendasLoja.tsx, `.vnd-*`), KPIs, filtros, multi-select, lançar lote/individual, aba Config Omie + sync SKU. Menu: Vendas (ShoppingBag) no grupo loja. Busca de menu inline sidebar: `fh-busca-menu-*` globals.css acima dos grupos, filtra itens ao digitar.

## PWA + PDV móvel + Compartilhar cardápio (homolog, deployado — só rebuild `web`)
- **Compartilhar cardápio**: `QrCardapioModal.tsx` virou "Compartilhar cardápio" — botão **Copiar link** (feedback Check), **Compartilhar** nativo (Web Share API, só aparece se `navigator.share`) e **WhatsApp** (`wa.me/?text=`), + download PNG/SVG e imprimir (banner/mesa). CSS `.qrc-*` em `app/loja.css`. Aberto pelo botão "QR" em `OperacoesLoja.tsx` (por operação com slug). Usa endpoint existente `qrcodeCardapio(slug)` (`/loja-pedidos/operacoes/:slug/qrcode`) — QR aponta p/ `${base}/cardapio/:slug`.
- **PWA** (instalável + offline básico) = **FebraHub COMPLETO** (não só PDV): `public/manifest.webmanifest` `start_url`=`/`+`scope`=`/` (abre o app inteiro; entra no PDV por opção). ⚠️ **`id` MANTÉM `/pdv-movel`** de propósito: é a identidade com que os aparelhos JÁ instalaram o app — mudar o `id` faria o Chrome tratar como app NOVO e o instalado velho continuaria abrindo em `/pdv-movel`; manter o `id` faz a instalação existente atualizar o `start_url` para `/`. `sw.js` CACHE_VERSION=v4: navegação NÃO é cacheada (senão o SW "gruda" e serve `/pdv-movel` velho); cache só fallback offline do shell `/`. `next.config.ts`: manifest.webmanifest e sw.js AMBOS `Cache-Control: no-cache, must-revalidate` (senão start_url antigo fica preso até 1h). GOTCHA: um PWA já instalado pode precisar ser reinstalado no aparelho p/ pegar a nova identidade/ícones. `PromptInstalar` c/ props `rotulo`+`className`, renderizado no **Shell.tsx** (`.fh-pwa-install` em globals.css → banner em TODO o app) + no balcão móvel. ícones `public/icons/icon.svg` + `icon-maskable.svg`, service worker `public/sw.js` (network-first navegação, cache-first `/_next/static`+`/icons`, **nunca** `/api`; bump `CACHE_VERSION` invalida), `components/pwa/RegistradorSW.tsx` (registra `/sw.js` só em `NODE_ENV=production`, montado no root `layout.tsx`), `components/pwa/PromptInstalar.tsx` (`beforeinstallprompt` + dica iOS, dispensa em localStorage `febrahub:pwa-dispensado`). `layout.tsx` ganhou `metadata.manifest`/`appleWebApp` + `<RegistradorSW/>`. `next.config.ts` `headers()`: `/sw.js` no-cache + `Service-Worker-Allowed:/`, manifest cache 1h.
- **PDV móvel** = rota `/pdv-movel` (route group PRÓPRIO fora de `(app)` — NÃO usa o Shell desktop). `app/pdv-movel/layout.tsx` = shell mobile (tema escuro fixo `.pm-*` em `app/pdv-movel/pdv-movel.css`) com **tab bar** inferior (Vender/Fila/Retirada), auth gate (redirect `/login`) + exige `loja.pedidos.operar`. Telas: **vender** (busca + chips categoria + grid produtos + sheet do carrinho com stepper + formas Dinheiro/Cartão/PIX; PIX = checkout→iniciarPagamento→poll 3s; reusa `lojaProdutosBalcao`/`vendaPdvFila`/`checkout`/`iniciarPagamento`/`lojaPedido`/`confirmarPagamento`), **fila** (SSE+polling, ações por status), **retirada** (reusa `RetiradaLoja` dentro de `.pm-scanwrap` que remapeia `--card/--bg/...`→`--pm-*` p/ herdar o tema escuro; esconde o hero). Root `/pdv-movel` redireciona p/ `/vender`. **Layout repolido** (deployado): topo c/ logo Febracis + botão voltar (`.pm-top-sair`), busca c/ limpar `.pm-busca-x`, chips com fade, grid 2→3col, **card img `object-fit:contain`** (era cover, cortava PNG), carrinho/cobrar CTA c/ sombra, sheets/modais c/ grabber. GOTCHA: campo `emDestaque` (destaques do balcão móvel) DEVE estar em `types/pdv.ts` `PdvProduto` senão `next build` quebra no type-check.
- Menu `loja-pdv-movel` "PDV móvel (app)" (após retirada, `loja.pedidos.operar`) + ação spotlight (ícone `Smartphone`, registrado em `BuscaGlobal.tsx` ICONES).
- **GOTCHA que quebrou o build Docker**: `tsc` NÃO resolve import de `.css` — só o webpack (next build). O import de CSS colocado em subpasta precisa do caminho completo `@/app/pdv-movel/pdv-movel.css` (não `@/app/pdv-movel.css`). Validar SEMPRE com `next build` na IdeaPad, não só `tsc`. **IdeaPad é `-p 2222` (porta!)** — sem a porta o SSH dá "Permission denied".

## UX fixes (rodadas 1-5, deployado homolog) — padrões reusáveis
- **Busca global (Spotlight)**: componente CONTROLADO — `useBuscaGlobal()` (no Shell) é o único dono do estado; `BuscaGlobal` recebe `aberto`+`onFechar`. Atalho **Ctrl+K**. Mapa `ICONES` no BuscaGlobal DEVE conter todo ícone citado em `acoes-catalogo.ts` (senão cai no fallback seta).
- **Modais reusáveis**: `components/ui/ModalPrompt.tsx` (coleta texto, validação, Enter/Esc, flag `perigo`) e `ModalConfirmar.tsx` — USE no lugar de `prompt()`/`window.confirm()` nativos. Padrão: estado `<X>Alvo`/`setX(item)` no botão + modal no fim + `mutate(arg,{onSuccess:()=>setX(null)})`.
- **Estados de erro**: sempre tratar `isError` (senão erro vira "lista vazia" silenciosa) e `onError` em toda mutation. `components/ui/Estado.tsx` (carregando/erro/vazio); feedback INLINE (sem toast) — `.co-error`/`.fin-empty`/`.loja-empty`/`.ped-feedback`/`.com-vazio`.
- **Campo UUID cru → autocomplete**: onde havia input de UUID colado à mão, usar autocomplete com debounce 300ms (ex.: Pedagógico Solicitações usa `pedagogico.matriculas({busca,porPagina:8})`, CSS `.ped-aluno-*`).
- Filtros server-side só aceitam UUID; p/ filtrar por nome, filtrar client-side o que a página trouxe. Coalescência `data?.itens ?? []` DENTRO do `useMemo`.
- PENDÊNCIAS honestas (não bugs): `HubEstoque` não baixa estoque auto em saída/entrada/devolução; `CadastrosFinanceiro` sem editar/excluir (API não expõe).

## Repaginada visual (identidade Febracis) — deployado homolog
Heros de módulo (`*-hero`): gradiente dourado `.09`+radius 16px+barra acento 3px+h1 26px. Índigo/azul hardcoded fora da identidade migrado p/ dourado (`var(--gold)`). GOTCHA: rebuild web ~20-30min; CSS novo pode parecer não-aplicado por CACHE do browser — cheque via `getComputedStyle`.

## Repaginada visual do PDV/Balcão (ref Figma POS Dashboard) — deployado homolog
Baseado em https://figma.com/design/yiy7wWKeOnskw6wt1jNXzJ (node 8206-18270, "Kopag POS"). NÃO copiado (design é azul/restaurante) — adaptados os PRINCÍPIOS de qualidade à identidade dourada Febracis, só via `apps/web/src/app/balcao.css` (CSS puro, SEM tocar a lógica/markup do BalcaoPdv que é território ativo do JABSON — F-keys/pagamento/estoque).
- Chips ativos/`kbd` dos atalhos: eram azul-marinho hardcoded → gradiente dourado `--gold-top/base`.
- Cards de produto: sombra premium sutil (`0 1px 3px` base, `0 10px 28px` hover) + borda dourada no hover, radius 14px.
- Steppers do carrinho: viraram botões circulares (− neutro / qtd / **+ dourado preenchido**), inspirado no ref.
- Total do carrinho: `row.grande` 24px com borda tracejada em cima e valor em `--gold` (destaque tipo "Total $187" do ref).
- GOTCHA colaboração: pode haver WIP não-commitado de outros no working tree — `git stash push <paths>` só dos seus arquivos antes de commitar. NUNCA commitar WIP alheio.

## LOJA — Código retirada 3 díg + impressão térmica MP-4200 TH (validado IdeaPad, deploy pendente)
Cliente recebe **código secreto de 3 díg** (100–999) no checkout; vendedor DIGITA em `/loja/codigo` → vê carrinho, edita (itens/qtd/obs/desconto) e **imprime** cupom na térmica. Migration `49_loja_codigo_retirada` + `LojaPedido.codigoRetirada Int?`.
- **codigoRetirada ≠ senhaFila**: código é PRIVADO (só comprovante do cliente); senhaFila é PÚBLICO (TV). `acompanhar()`/`painelTv()` fazem select sem ele; só `comprovanteBase()` expõe (`codigo`). Gerador `proximoCodigoRetirada` (advisory lock `loja_codigo_<op>`, aleatório, único só entre pedidos ATIVOS→reaproveitável; null se 900 ocupados). Gerado no checkout e vendaPdv ENVIAR_PREPARACAO.
- **Endpoints** (`loja.pedidos.operar`): `GET codigo/:codigo`, `PATCH pedidos/:id/itens` (EditarItensDto: substitui itens+desconto, revalida estoque, ajusta reserva/físico p/ status; editável AGUARDANDO_PAGAMENTO/NA_FILA/PROXIMO/EM_PREPARACAO), `POST pedidos/:id/imprimir`, `GET impressora/status`.
- **Impressora** `impressora.service.ts` → servidor na IdeaPad (env `PRINTER_URL`/`IMPRESSORA_URL`, default `http://172.17.0.1:9100`; `PRINTER_TIMEOUT_MS`). Contrato: `GET /health`{ok,device,writable}; `POST /print/raw`{data:base64} (usado); `POST /print/text`{text}.
- **⚠️ MP-4200 TH = modo ESC/Bematech, NÃO ESC/POS** (cmd ESC/POS saem como texto). `cupom-escpos.ts`: fonte=**`ESC ! n`** (1B 21 n: 0x08 negrito/0x10 dupla-alt/0x20 dupla-larg), NÃO `GS !`; corte=**`ESC i`** (1B 69), NÃO `GS V`; **QR/barcode NÃO funcionam**→só texto; acentos→ASCII; 48 col.
- **Ticket = PREPARO + RECIBO** (vendedor imprime, anexa ao pedido): FEBRACIS+op, SENHA grande, itens+preço, subtotal/desconto/TOTAL/pagto, rodapé endereço Pituba+tel (env `LOJA_ENDERECO`/`LOJA_TELEFONE`). NÃO imprime o código secreto.
- **Web**: `AtenderCodigo.tsx`+`/loja/codigo` (`app/codigo.css` `.cod-*`); cliente vê código em `AcompanharPedido.tsx` (`.cmp-codigo`). Client: `buscarPedidoPorCodigo/editarItensPedido/imprimirCupomPedido/impressoraStatus`. Menu `loja-codigo`+spotlight (KeyRound). Fix: `sympla.ts` `./api`→`./client`+`{parametros}`.

## Select custom (dropdown estilizado) — substitui <select> nativo
`components/ui/Select.tsx`: dropdown próprio (button+painel), dourado, tema, teclado (↑↓ Home End Enter Esc, digitar-pra-pular), a11y (role=listbox), fecha fora/Esc. API `value`+`onChange(v)`+`options:[{value,label}]` (+placeholder/disabled/className/style/aria-label). **DROP-IN**: passe a mesma classe que o `<select>` tinha (largura) — o wrapper `.fh-select` ZERA border/bg/padding de classes legadas via `!important` (CSS `.fh-select*` em globals.css). onChange dá `string` (cast no callback se precisar). **USE-O no lugar de `<select>` em telas novas.** ROLLOUT ~COMPLETO (48 arquivos): comercial, pedagógico, processos, financeiro, organograma, compras, loja (Catálogo/Balcão/Estoque/Vendas/Auditoria/Operações/Dashboard/Inventário), pdv (Frente/Vendas/móvel), permissões, notificações, executivo, crm, fiscal, social, brain, territorial + `CampoFormulario` (propaga a todo FormCrud). Restam só 2 nativos INTENCIONAIS: `DetalheCompra` Estoque sub-form (`name=produto-${id}` FormData dinâmico) e `KnowledgeGraph` (config do grafo) — baixo tráfego, deixados. GOTCHA: `Select value` exige `string` — coalesça `undefined→''`/default (ex.: `f.unidade ?? 'un'`). Forms FormData: troque `name=` por estado controlado + leia do estado no submit (não de `FormData`). GOTCHA COLAB: NÃO deixar WIP descommitado — `git add -A` de outro agente VARRE seu WIP p/ o commit dele. Commite logo, arquivos específicos (`git add <paths>`).

## Estoque — aba Inventário (deployado homolog)
`/estoque` (`EstoqueGeral.tsx`) tem 3ª aba **Inventário** → `components/loja/InventarioLoja.tsx`: bipar código/QR pela câmera (reusa `LeitorQr` html5-qrcode; `normalizarCodigo()` extrai dígitos de URL) → `lojaBuscarPorBarcode` (404→busca nome) → **contagem por local** aplica ajuste `tipo:'inventario'` (saldo absoluto, `lojaAjustarEstoque`); **trocar foto** (câmera/arquivo, `lojaAtualizarProduto` payload COMPLETO); associar/limpar barcode; histórico `lojaMovimentos`. Só edita com `loja.produtos.gerenciar`. Backend já existia. CSS `.inv-*` em `loja.css`. GOTCHA: `--linha`/`--card2` NÃO são tokens (usar `--card-line`). (PWA global = ver seção "PWA + PDV móvel".)

## Padrão SOLID/testes — regras puras extraídas de services (loja-pedidos é a referência)
Para reduzir "God Services" sem arriscar transações homologadas: extraia a **lógica de negócio PURA** (aritmética, formatação, veredito) para `modules/<mod>/regras/*.ts` com `*.spec.ts` ao lado; o service vira orquestração + acesso a dados e **delega** às funções puras. NÃO mover `$transaction`/advisory locks/idempotência para outro service sem testes de concorrência (risco de oversell/senha duplicada).
- **Feito em `loja-pedidos/regras/`**: `mensagens.ts` (fmtSenha, mensagemRegua, mensagemLembretePronto), `retirada.ts` (gerarTokenRetirada, renderizarQr, urlRetirada, avaliarRetirada→veredito pago/retirado/podeRetirar/bloqueio), `calculos.ts` (round2, totalLinha, somarSubtotal, descontoValido, totalComDesconto, disponivel, estoqueInsuficiente, conferirSplit split PDV, parseCodigoRetirada, escolherCodigoRetirada rng-injetável, mediaMinutos 1-casa). Service 1438→1377 linhas; delega em checkout/vendaPdv/editarItens/comprovante/consultarRetirada/dashboard/buscarPorCodigo/qrCodeCardapio/proximoCodigoRetirada/lembrarProntos. Comportamento idêntico (validado).
- **GOTCHA equivalência**: ao extrair, preserve saída EXATA — `mediaMinutos` arredonda a **1 casa** (não 2) porque o dashboard usava `.toFixed(1)`; `escolherCodigoRetirada` recebe `rng` p/ teste determinístico.
- **Validação (sempre na IdeaPad)**: `npx jest src/modules/<mod>/regras` (specs puros, sem DB, ~13s) + `npx tsc --noEmit` (gate real) + `npx nest build`. ⚠️ **ESLint NÃO está wired** (ESLint 9 sem `eslint.config.js` em lugar nenhum → `pnpm lint` quebra p/ todos); tsconfig NÃO tem `noUnusedLocals` → imports não-usados não são pegos por tsc, limpe à mão. `.spec.ts` compilam p/ `dist` (convenção já existente no repo — executivo/processos/auth/permissoes; Nest não os carrega em runtime).

## Marketing — Instagram: usar SÓ o Zernio (aiograpi REVERTIDO)
O Zernio já cobre o Instagram oficial (publicação, análise, DMs, campanhas Meta) sem violar ToS — rede first-class dele (`social/comum.tsx`, `social.service.ts` lista `instagram`). A integração por **login direto via aiograpi-rest** (API privada, risco de ban) foi **descartada/revertida**: migration `52` criou `instagram_config`, migration `54` DROPA; `modules/instagram/`, `AbaInstagram.tsx`, `services/api/instagram.ts`, serviço `instagram` do compose e `ALOOK_AIOGRAPI_URL` removidos. **NÃO reintroduzir** — "Instagram no marketing" = Zernio. (Zernio grátis nos 2 primeiros perfis, depois ~$1-6/perfil/mês.)

## Estrutura da raiz (reorg — o que NÃO mover)
Docs de referência: manual bematech em `docs/`, planilhas-fonte em `docs/referencias/`; capas de livros one-off em `tools/capas-livros/*.mjs`. **NÃO mover da raiz** (quebra deploy/pnpm): `deploy.sh`+`docker-compose.prod.yml` (deploy na 66 usa `cd /root/FebraHub && ./deploy.sh`), `package.json`/`pnpm-*.yaml`/`.env.example`, `patches/` (path absoluto no lockfile → `--frozen-lockfile` quebra). `graphify-out/`, `db/`, `etl/` (Dockerfile próprio), `supabase/migrations`, `infra/`, `tools/` seguem top-level.

## Referências de UX + PageHeader
`docs/references/` (gitignored, ~660MB, só consulta): `concessionaria` (DMS Next+MUI, ref de layout/UX), `citybox` (origem fiscal-api), `team`. **MUI já instalado** (`@mui/material` 9 + `ProvedorMui` com paleta dourada Febracis), mas hoje só nos módulos de conversas/agentes; o resto (~290 telas) usa tokens CSS próprios (`lib/tema.ts`+`globals.css`). Migração total pra Material Design foi ADIADA (decisão de produto; alto risco com trabalho paralelo). **`components/ui/CabecalhoPagina.tsx`** = PageHeader padrão (eyebrow+título+descrição+slot `acoes`) sobre `.fh-page-*`; usar no lugar de `<h1>+<p>+botões` inline (adotado Comercial). **`components/ui/TabelaDados.tsx`** = tabela paginada sobre `@tanstack/react-table` (já instalado): `dados`+`colunas` (ColumnDef) → paginação client + ordenação (CSS `.fh-tabd*`). Usar em tabelas que renderizam a lista toda e estouram verticalmente (adotado AuditoriaLoja, Solicitações). Paginação SERVIDOR = `PaginaCrud`.
