/**
 * Multiplicador MUI `sx` para `theme.shape.borderRadius`
 * (`1` = raio do tema, ex. 8px no preset comércio).
 *
 * @see productFormBorderRadius em `features/products/lib/product-form-section-styles.ts`
 */
export const surfaceBorderRadius = 1;
