'use client';

/**
 * @deprecated Prefer `@/features/reminders/hooks/use-reminders-query`.
 * Mantido para imports existentes em leads/header.
 */
export { useLeadsReminders } from '@/features/reminders/hooks/use-reminders-query';
