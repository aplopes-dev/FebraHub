import type { StoreSummary } from '../types/store';

/** Store mock até existir seleção de loja / sessão. */
export const PLACEHOLDER_STORE: StoreSummary = {
  id: 'store-placeholder',
  name: 'Minha Loja',
  logoUrl: null,
  address: 'Rua Exemplo, 100 — Centro, Ilhéus, BA, 45650-000',
};
