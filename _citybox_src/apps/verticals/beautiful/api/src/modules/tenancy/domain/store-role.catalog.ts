/**
 * Reexporta papéis lean do package CASL (fonte única).
 * Mantido neste path para imports históricos da API.
 */
export {
  STORE_ROLES,
  SCHEDULABLE_STORE_ROLES,
  isStoreRole,
  isSchedulableStoreRole,
  storeRoleLabel,
  type StoreRoleId,
  type SchedulableStoreRoleId,
} from '@citybox/beautiful-permissions';
