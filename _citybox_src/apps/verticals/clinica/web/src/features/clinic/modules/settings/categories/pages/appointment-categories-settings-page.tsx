'use client';

import { AppointmentCategoriesSection } from '../components/appointment-categories-section';

/** Configurações — categorias de agendamento. */
export function AppointmentCategoriesSettingsContent() {
  return <AppointmentCategoriesSection />;
}
