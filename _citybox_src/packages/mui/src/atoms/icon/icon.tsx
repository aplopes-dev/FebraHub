"use client";

/**
 * @deprecated Use `Icon` de `@citybox/mui/icons` com `name` semântico.
 * Reexport mantido para não quebrar imports antigos de `@citybox/mui/atoms`.
 */
export { Icon, type IconProps } from "../../icons";
