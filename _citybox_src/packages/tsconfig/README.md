# @citybox/tsconfig

Presets TypeScript: `base.json` (strict ESM), `nest.json` (APIs), `react.json` (Next.js).

## Papel no projeto

- **Alinhamento:** mesmo `strict`, `moduleResolution` e paths em todos packages.

## Referências

- [packages/README.md](../README.md)
