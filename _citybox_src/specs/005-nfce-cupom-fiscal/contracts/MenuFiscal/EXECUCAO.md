# Execução — Menu Fiscal (11 features)

Ordem = dependência (não do menu). Não iniciar N+1 antes de N estar CONCLUÍDA e verde.
Exceção: 011 e 017 são independentes e podem ser antecipadas se 010 travar.

| # | Feature | Branch | Spec | Status | Conferência | Observações |
|---|---------|--------|------|--------|-------------|-------------|
| 010 | Emissão de Certificado → fiscal-certificate-screen | feat/fiscal-api | specs/erp/010-fiscal-certificate-screen | CONCLUÍDA | OK (5 camadas, abaixo) | erp-web (tela Fiscal, feature `fiscal-certificate`) + `platformStoreId` no presenter erp-api (testado). Front sem testes (sem harness, decisão D0). Reviews react/ts/security aplicadas (1 CRITICAL do TS-review corrigido: wrapper `{data}` do POST /companies). Não commitado. |
| 011 | Serie de NF → fiscal-invoice-series | feat/fiscal-api | specs/erp/011-fiscal-invoice-series | CONCLUÍDA | OK (5 camadas) | Backend `fiscal-sequences` (5 rotas CRUD) + migration auditoria + `active` bloqueia emissão + abas em /configuracoes/fiscal (Certificado+Séries) + aba Séries. Gates verdes (fiscal-api typecheck/lint/build/13 testes; erp-web typecheck/lint/build). **Reviewers**: database (HIGH atomicidade da auditoria → CORRIGIDO com $transaction), typescript (2 MEDIUM → corrigidos), react (3 HIGH → 2 meus corrigidos + 1 no RowActionsMenu compartilhado corrigido; a11y), security (2 HIGH **pré-existentes na emissão** → B4/B5 abaixo, fora do escopo de 011). Não commitado. |
| 012 | Configurações gerais → fiscal-general-settings | feat/fiscal-api | specs/erp/012-fiscal-general-settings | CONCLUÍDA | OK (5 camadas) | Aba "Configurações gerais" (3ª aba de /configuracoes/fiscal): regime/IE/IM/ambiente/autXML/NFS-e nacional (`PATCH /v1/companies/:id`) + **CSC write-only** (`PUT …/csc`, cadastra o CSC que a 013 exige) + campos sem backend "em breve" (backlog D1: fiscal-api×erp-api). Backend: contrato de update explícito (accountingOfficeDocument+nationalNfseEnabled) + teste (3/3). Gates verdes. Reviewers: security PASS; react 2 HIGH (perda de dados por remount + a11y tabpanel) → corrigidos; typescript 2 MEDIUM → corrigidos. Não commitado. |
| 013 | Tipo de NF emitida(PDV) → pos-fiscal-document-type | feat/fiscal-api | specs/erp/013-pos-fiscal-document-type | CONCLUÍDA (config) · PDV DEFERIDO | OK (5 camadas) | **Config entregue e verde**: erp-api `pos-fiscal-settings` (entidade por org, GET/PUT gestão + GET device, migration `pos_fiscal_settings` — ⚠️ **não aplicada aqui, DB erp não provisionado; aplica via migrate deploy**, 4 testes) + aba erp-web "Tipo de NF (PDV)" (bloqueio Modelo 65 sem CSC, aviso certificado, ICMS toggle desabilitado). Gates verdes (erp-api typecheck/test; erp-web typecheck/lint/build). Reviewers: database CLEAN; security PASS; react 2 HIGH (cache CSC divergente + erro engolido) → corrigidos; typescript 2 HIGH (Error cru→ValidatorDomainError; cast→type guard) → corrigidos. ⚠️ **Consumo/emissão no PDV DEFERIDO** (B7): app-alvo `apps/pdv/frontend` sem código; PDV real é Flutter `apps/pdv/app`/legado `apps/pdv/legado`, sem integração fiscal — feature própria (outra stack). Config exposta por `GET /v1/pos/fiscal-settings`. Não commitado. |
| 014 | Padrões fiscais → fiscal-default-taxes | feat/fiscal-api | specs/erp/014-fiscal-default-taxes | CONCLUÍDA | OK (5 camadas) | erp-api módulo `fiscal-defaults` (`FiscalGroup` leitura + `FiscalDefaultTaxes` get/put por org, valida grupo por org+tributo→422) + `issqn` em `ProductFiscal`/`ProductFiscalBranch` (pipeline `/v1/fiscal-parameters` inteiro) + migration `20260813150000_fiscal_default_taxes` (⚠️ **não aplicada** — DB `citybox_platform` não provisionado; `migrate deploy`). erp-web aba "Padrões fiscais" (`?aba=padroes`) + ISSQN e **herança** ("Herdado do padrão: …") em fiscal-parameters. Decisões: plan D1 (CFOP=código catálogo estático), D4 (write `store.catalog.manage`). Sem fiscal-api/XML. Gates verdes: erp-api typecheck/lint/testes (12/12 meus; 11 falhas do suite DB-backed pré-existente); erp-web typecheck/lint/build. Front sem testes (D0). **Reviewers**: database (2 HIGH → índices nas 4 FKs de grupo + CHECK de tax_type; cross-tenant FK já mitigado pela validação no use case), security (1 MEDIUM → `FiscalGroup`/`FiscalDefaultTaxes`/`PosFiscalSettings` registrados em `TENANT_SCOPED_MODELS`; 1 HIGH **pré-existente** catalog sem `@RequirePermission` → corrigidas as 3 rotas de fiscal-parameters que a 014 toca, resto → B8), react (2 HIGH → queryKey escopada por org + gate `isPending` contra falso-erro na hidratação; 1 MEDIUM → rótulo humano no estado vazio), typescript (1 HIGH → upsert chaveado por `organizationId` (não `id`) contra P2002 na 1ª gravação concorrente; MEDIUM → validações em `Promise.all` + `findUnique`; LOW → comentário no cast). Não commitado. |
| 015 | Grupo PIS-Confins → fiscal-pis-cofins-group | feat/fiscal-api | specs/erp/015-fiscal-pis-cofins-group | CONCLUÍDA | OK (5 camadas) | **Fatia vertical em 3 serviços, TOCA O XML.** **fiscal-api**: `buildPisCofinsXml` apura de verdade (`NfeItemInput.pis`/`cofins`; PISAliq/COFINSAliq calculado, PISNT/COFINSNT, totais somados; Simples CST 49 e fallback CST 01 intactos) — **30 builder tests** (prova SC-003 + 2 não-regressões, contra XSD real). **erp-api**: `FiscalGroup` estendido (regra PIS/COFINS + create/update) + módulo CRUD `v1/fiscal-pis-cofins-groups` + `ResolveItemPisCofinsUseCase` (produto→grupo→padrão→fallback) + `ProductFiscal.pisCofinsGroupId` FK (validado na escrita) — 22 testes jest. **erp-web**: feature `fiscal-pis-cofins-group` (rota própria D3, form com CST/alíquota condicional + espelhamento + prefill por regime + aba Produtos). Decisões plan D1–D6. Gates verdes (3 pacotes: typecheck/lint/test; erp-web build). Front sem testes (D0). **Reviewers**: database (1 HIGH FK cross-tenant → validação app-layer na escrita, composite FK inviável com SetNull), security (1 HIGH = mesma, já corrigida + 1 LOW `@IsUUID` aplicado + 1 MEDIUM fiscal-api validar CST/alíquota → **B10**, dormant até B7), react (1 CRITICAL markup `<a>` em `<tbody>` → stretched-link + 3 HIGH: espelhamento não sobrescreve alíquota divergida / `type=text` vírgula pt-BR / valida alíquota tributada no submit → todos corrigidos), typescript (1 HIGH GetFiscalGroup sem guard de taxType → corrigido; MEDIUM arredondamento base dos totais / CST `string`→união / merge de `pisCofinsGroupId` no upsert → corrigidos). **Emissão real (disparo PDV→fiscal-api) = B7 deferido** — entregamos resolvedor + contrato + builder. Não commitado. |
| 016 | Grupo ICMS → fiscal-icms-group | feat/fiscal-api | specs/erp/016-fiscal-icms-group | CONCLUÍDA | OK (5 camadas) | **Fatia vertical em 3 serviços — resolve B1.** **fiscal-api**: `buildImpostoXml` apura ICMS real (`ICMS00` com `orig`/`vBC`/`pICMS`/`vICMS` da UF de destino; totais somados; Simples `ICMSSN` e fallback zerado intactos) — **35 builder tests** (prova SC-004 + não-regressões). **erp-api**: `FiscalGroup` += situação ICMS (CST 00 / CSOSN 102/103/300/400, "exatamente uma") + **tabela filha `FiscalGroupUfRate`** (grupo×UF×tipo, dedup/normalização) + CRUD `v1/fiscal-icms-groups` + `ResolveItemIcms` (interna se destino=UF emitente, senão interestadual; herança; fallback) + `ProductFiscal.icmsGroupId` FK validado na escrita — 35 testes jest. **erp-web**: feature `fiscal-icms-group` (rota própria D3, form com situação por regime + 2 matrizes de 27 UFs c/ alternância valor único/personalizado + aviso do limite no Simples). Decisões plan D1–D8. Gates verdes (3 pacotes: typecheck/lint/test; erp-web build). Front sem testes (D0). **Reviewers**: database (CLEAN — 2 LOW consistentes c/ padrão), security (1 MEDIUM → 3 testes cross-tenant do `icmsGroupId` add + 1 LOW → `@IsIn` das 27 UFs), react (**3 HIGH** perda silenciosa de alíquota: custom→single semeia o valor / valida alíquota no submit p/ CST / regime `isError` para de cair no default → todos corrigidos; 2 MEDIUM `router.refresh` removido + Select required), typescript (**1 HIGH** total ICMS divergia do `det` quando item de Regime Normal trazia `csosn` perdido → `icmsItemValue` deixou de checar csosn; MEDIUM matriz na edição sem linhas → modo personalizado; LOW comentário do cast). **Emissão real no PDV = B7 deferido** (resolvedor + contrato + builder entregues). Não commitado. |
| 017 | Informações adicionais NF-s → fiscal-additional-info | feat/fiscal-api | specs/erp/017-fiscal-additional-info | CONCLUÍDA | OK (5 camadas, abaixo) | **Fatia vertical em 3 serviços, TOCA O XML. Bloqueio de XSD resolvido pela decisão do usuário ("continue" = opção A).** Achado: o `.txt` supunha `infAdic`/`infAdFisco` nas 3, mas o `DPS_v1.01.xsd` da NFS-e **não tem** `infAdic`/`infAdFisco` — só `serv/infoCompl/xInfComp` (análogo do `infCpl`). **Opção A (plan D10)**: NF-e/NFC-e emitem `infAdic` (infAdFisco+infCpl); NFS-e emite só `xInfComp`; destino `INF_AD_FISCO` é **indisponível** na NFS-e (recusado na entidade + CHECK no banco). **fiscal-api**: `buildInfAdicXml` (NF-e/NFC-e, via `buildNfeXml` — cobre os dois modelos) + `buildInfoComplXml` (DPS, `serv/infoCompl/xInfComp`); omitido sem texto (não-regressão); reforça teto do XSD + rejeita caractere de controle ilegal XML 1.0 antes de transmitir — **62 builder tests** (infAdic/infoCompl validados contra XSD real + escaping `<>&` + controle ilegal + não-regressões). **erp-api**: módulo `fiscal-additional-info` (entidade + repo prisma/in-memory + CRUD `v1/fiscal-additional-infos` + `ResolveDocumentAdditionalInfoUseCase` — concatena por (tipo,destino) na ordem de criação com desempate por `id`, valida total ≤ teto → **impede, não trunca**; `documentType` imutável; NFSE+INF_AD_FISCO e controle ilegal recusados) + migration `20260813180000_fiscal_additional_info` (⚠️ **não aplicada** — DB `citybox_platform` não provisionado; `migrate deploy`; CHECK de `document_type`/`target`/regra D10) + `TENANT_SCOPED_MODELS` — **16 testes jest**. **erp-web**: feature `fiscal-additional-info` (rota própria D3, **abas por tipo na URL** `?tipo=` + Dialog criar/editar sem toggle D5, Destino desabilita INF_AD_FISCO na NFS-e, `canSave` bloqueia texto acima do teto do destino + aviso inline, a11y tabpanel, Suspense, estado vazio no-data/no-match, busca reseta ao trocar aba) + GUIA.md + link na aba Padrões fiscais. Decisões plan D1–D10. Gates verdes (3 pacotes: fiscal-api typecheck/lint/62 testes; erp-api typecheck/lint/16 testes; erp-web typecheck/lint). Front sem testes (D0). ⚠️ erp-api: 2 suites **DB-integration** (`prisma-sale-order…card-settlement`) falham por falta de Postgres no ambiente (pré-existente, não regressão; 1005 passam). **Reviewers (todos aplicados)**: database (consistente/correto, 1 MEDIUM → CHECK constraints de enum + regra D10 no banco), typescript (aprovado, 1 MEDIUM → desempate por `id` na concatenação contra empate de ms + 2 LOW → query-DTO valida enum (400) / cast `as AdditionalInfoTarget`), security (sem CRITICAL/HIGH; tenant/authz/no-trunc sólidos; 1 MEDIUM → rejeição de caractere de controle ilegal XML 1.0 nas **2 camadas** (entidade + builders) + 1 LOW → testes de escaping), react (**1 HIGH** → `canSave` não barrava texto acima do teto ao trocar destino → gating + feedback inline; **4 MEDIUM** → a11y tabpanel / Suspense boundary / estado vazio no-data×no-match / reset de busca na troca de aba → todos corrigidos). **Emissão real (disparo PDV→fiscal-api) = B7 deferido** — entregamos resolvedor + contrato + builder. Não commitado. |
| 018 | Grupo ISSQN → nfse-issuance-with-issqn-groups | feat/fiscal-api | specs/erp/018-nfse-issuance-with-issqn-groups | CONCLUÍDA | OK (5 camadas, abaixo) | **Fatia grande — LIGA O ERP À EMISSÃO DE NFS-e da fiscal-api (que ninguém acionava).** 3 partes: cadastro de Grupos de ISSQN + tela de emissão + integração erp-api→fiscal-api. **fiscal-api**: `tribISSQN` **deixa de ser fixo '1'** — `DpsServiceInput.tribISSQN?` (default '1', não-regressão) threadado no builder **e** no caminho de emissão (`IssueNfseDto` HTTP+app + `IssueNfseUseCase`); `pAliq` só com retenção intacto (não-regressão E0625). Builder DPS **26/26** + issue-nfse **13/13**. **erp-api**: `FiscalGroup` taxType=ISSQN (`issqn_service_code`/`national_code`/`rate`/`trib_type`, CST 1/2/4; 3 exportação fora) + CRUD `v1/fiscal-issqn-groups` + `ResolveServiceIssqnUseCase` + **`ProductFiscal.issqnGroupId`** (FK validada na escrita, `assertGroupOfType('ISSQN')`) + módulo **`nfse-issuance`** (emite via `FiscalApiClient` server-to-server, idempotência local, permissão nova **`store.fiscal.issue`**, vínculo `NfseIssuance`). Migration `20260813190000_nfse_issqn_groups` (⚠️ `migrate deploy`; CHECK tribISSQN + índices). Jest: issqn-group 9/9, upsert-fiscal-parameters 13/13, issue-nfse 7/7. **erp-web**: feature `fiscal-issqn-group` (rota própria `/configuracoes/fiscal/grupos-issqn`) + **`nfse-issuance`** (1ª tela que emite NFS-e, `/vendas/nfse`: tomador+grupo+valor+retenção, guarda de Emitente, HOMOLOGAÇÃO, confirmação, erros do órgão traduzidos). Gates: fiscal-api **618/0**; erp-api **1024** passam (**regressão pré-existente `tenant-scope.extension.spec` — allowlist desatualizada desde 015 — CORRIGIDA**; só 1 suite DB-integration falha por falta de Postgres, ambiente). erp-web tsc/lint limpos; sem testes de front (D0). **Reviewers (todos aplicados)**: database (CRITICAL nome de índice unique 83>63 bytes → `map:` curto; HIGH índice de ordenação `created_at`), typescript (HIGH corrida de idempotência → catch do unique + re-leitura; LOW cast tribISSQN → tipo `IssqnTribType`; LOW token dev), react (HIGH estados de erro silenciosos das queries → alerts distintos; MEDIUM `ConfirmationDialog loading`; MEDIUM toast no guard; LOW maxLength), **security (CRITICAL — cross-tenant emission: `companyId` vinha do cliente → agora RESOLVIDO server-side pelo CNPJ da própria org; MEDIUM token dev fail-fast em produção; LOW vazamento de host na msg de rede; LOW corrida idempotência)**. ⚠️ **Pré-requisito operacional (produção, não código): IM da empresa no CNC do município (E0116)** — validado em HOMOLOGAÇÃO. Ancoragem Vendas/OS + produção fora de escopo. Não commitado. |
| 019 | Grupo do IPI → fiscal-ipi-group | feat/fiscal-api | specs/erp/019-fiscal-ipi-group | CONCLUÍDA | OK (5 camadas) | **Fatia vertical em 3 serviços, TOCA O XML — última das telas de grupos fiscais.** **fiscal-api**: `buildImpostoXml` passa a emitir o grupo `IPI` do item (`NfeItemInput.ipi = {cst, cEnq, aliquota?}`): `cEnq` + `IPITrib` (CST 50/99 tributado, com `vBC`/`pIPI`/`vIPI`) ou `IPINT` (51–55, sem valores), inserido **entre ICMS e PIS/COFINS** na `xs:sequence`; total `vIPI` **deixa de ser `0.00` fixo** e soma os itens tributados; IPI é "por fora" → integra `vNF`. **47 builder tests** (IPITrib/IPINT validados contra XSD real + CST 99 + soma de totais + **não-regressão SC-005**: item sem `ipi` → sem bloco, `vIPI 0.00`, `vNF` inalterado). **erp-api**: `FiscalGroup` taxType=IPI (`ipi_cst` saída 50/51/52/53/54/55/99, `ipi_enquadramento` `cEnq`, `ipi_rate` — percentual só p/ tributado 50/99) + tabela **`cEnq` estática versionada** (`ipi-enquadramento.table.ts` + teste de imutabilidade c/ assertiva de código único) + `createIpi`/`updateIpi`/`validateIpi` + CRUD `v1/fiscal-ipi-groups` (`store.catalog.manage`) + `ResolveItemIpiUseCase` (item→grupo→`{cst,cEnq,aliquota}` **ou null** = sem bloco IPI) + **`ProductFiscal.ipiGroupId` + `ProductFiscalBranch.ipiGroupId`** (FK validada na escrita, `assertGroupOfType('IPI')`). Migration `20260813200000_fiscal_ipi_group` (⚠️ **não aplicada** — DB `citybox_platform` não provisionado; `migrate deploy`; CHECK de `ipi_cst` só saída + FKs indexadas). Jest: cEnq+upsert 19/19, fiscal-defaults+catalog **226/226** (só suites DB-integration falham por falta de Postgres, ambiente). **erp-web**: feature `fiscal-ipi-group` (rota própria `/configuracoes/fiscal/grupos-ipi` molde 016/018, form abas Configuração+Produtos, **Percentual condicional ao CST** (50/99), select `cEnq`, aba Produtos somente-leitura, `key={group.id}` no edit) + link na aba Padrões fiscais + GUIA.md. Gates verdes (fiscal-api 47; erp-api typecheck/226; erp-web typecheck/lint limpos). Front sem testes (D0). **Reviewers (todos aplicados)**: database (CLEAN — sem drift, CHECK correto, FK SetNull byte-a-byte igual aos irmãos, sem colisão de nome), typescript (2 MEDIUM → `cEnq` `999` duplicado removido + assertiva de unicidade; cobertura de `ipiGroupId` em upsert; 2 LOW null/undefined cross-service + constantes CST duplicadas = padrão aceito dos irmãos), react (1 HIGH → `key` no edit form contra baseline stale; 1 MEDIUM → **aba Produtos** que faltava wired; Cancelar/Back-bypass-guard = tradeoff aceito dos irmãos), security (CLEAN — sem injeção XML `cEnq`/CST allowlisted end-to-end, totais corretos, tenant isolation + permissões idênticos aos irmãos; 1 LOW → B7). **Emissão real no PDV = B7 deferido** (resolvedor + contrato + builder entregues; ao ligar, DTO HTTP da fiscal-api deve validar CST/`cEnq`). Não commitado. |
| 020 | Natureza de operações → fiscal-operation-natures | feat/fiscal-api | specs/erp/020-fiscal-operation-natures | CONCLUÍDA | OK (5 camadas, abaixo) | **Último da fila — fila fiscal 11/11.** Backend (erp-api módulo `operation-natures`) já existia acumulado no commit `989443cd0`, mas ficou fora do `tasks.md`/desta linha (achado ao retomar a sessão): entidade agregada `OperationNature` (cfopRules + groupRules discriminadas por `taxType`), tabela de CFOP estática versionada (`cfop.table.ts`, entrada 1xxx/2xxx × saída 5xxx/6xxx, teste de imutabilidade), `ResolveOperationNatureUseCase` (mais específico SIM/NÃO > AMBOS; não-casa → mantém original, não bloqueia; regra de grupo órfã ignorada), CRUD `v1/operation-natures` (`org.view`/`store.catalog.manage`), migration `operation_natures` + 2 filhas (FK `SetNull` nos grupos) — **36/36 testes verdes**. **Esta sessão entregou o frontend que faltava**: feature `fiscal-operation-natures` em `apps/erp/web` — rota própria `/configuracoes/fiscal/naturezas-operacao` (lista + `/novo` + `/[id]`), form com 4 blocos (Informações gerais com benefício fiscal desabilitado + 3 listas de-para com linhas adicionáveis: CFOP+ICMS Livre, Grupo de ICMS, Grupo de PIS/COFINS), `lib/cfop-options.ts` espelhando a tabela da erp-api, reusa `useFiscalGroupsQuery` de `fiscal-default-taxes`; link "Gerenciar naturezas de operação →" na aba Padrões fiscais. Gates verdes (erp-api 36 testes; erp-web typecheck/lint/build). Front sem testes (D0, herdado das features irmãs). **Reviewers**: database (nenhuma migration nova nesta sessão — schema já revisado quando o backend foi codado), react (1 HIGH → `useState` com valor plano em vez de lazy initializer reinvocava `initialState`/`nextRowKey` — efeito colateral no contador de row-key — a cada render; corrigido para `useState(() => initialState(nature))`; 1 LOW não bloqueante sobre Tooltip em Checkbox disabled), typescript (aprovado; contrato DTO↔backend conferido campo a campo incl. paridade byte-a-byte da tabela de CFOP; 1 MEDIUM não bloqueante — falta validação client-side de duplicata exata de CFOP antes do round-trip, backend já rejeita com mensagem clara, mesmo padrão dos irmãos; 1 LOW exports não usados em `cfop-options.ts`), security (CLEAN — cache escopado por org via `useCatalogScope`, `comercioFetch` único caminho HTTP, sem XSS, sem vazamento de erro). **Emissão real de entrada/devolução na fiscal-api = fora de escopo** (pré-requisito não implementado, mesma natureza do B7) — entregamos cadastro + resolvedor testados em isolamento. Não commitado. |

## Bugfixes independentes (agendar à parte, /bugfix + teste de regressão)

| ID | Onde | Defeito | Resolvido por | Status |
|----|------|---------|---------------|--------|
| B1 | services/fiscal-api `buildImpostoXml` | ICMS00 com pICMS/vICMS '0.00' hardcoded (Regime Normal) — nota fiscalmente errada | feature 016 | ✅ RESOLVIDO (016: `ICMS00` apurado com `vBC`/`pICMS`/`vICMS`/`orig` reais + totais somados; builder tests provam) |
| B2 | services/fiscal-api builder | aceita qualquer csosn; CSOSN 101 exige pCredSN/vCredICMSSN — XML inválido | — | PENDENTE |
| B3 | services/fiscal-api `FiscalSequence.active` | campo decorativo, nada consulta | feature 011 | ✅ RESOLVIDO (011: `active` bloqueia emissão) |
| B4 | services/fiscal-api `IssueNfeUseCase`/`IssueNfseUseCase` | `companyId` vem do corpo do request, **sem `CompanyAccessPolicy`** — caller com `fiscal.documents.manage` pode reservar/queimar numeração de outro Emitente (cross-tenant). NFC-e já foi corrigida (usa `X-Company-Id`+policy). Achado security-review (HIGH, pré-existente). | /bugfix próprio | PENDENTE |
| B5 | services/fiscal-api `reserveNextNumber` (nfe/nfce/nfse) + `prisma-fiscal-sequence.repository` | Reserva de número é read-modify-write **sem lock/transação**; duas emissões concorrentes na mesma série podem gerar número fiscal duplicado (sem unique constraint em `FiscalDocument(company,type,series,environment,number)` de backstop). Achado security-review (HIGH, pré-existente). | /bugfix próprio | PENDENTE |
| B7 | PDV (`apps/pdv/app` Flutter / `apps/pdv/legado`) | **Consumo/emissão fiscal no fechamento de venda** (feature 013 FR-101/102/103): ler `GET /v1/pos/fiscal-settings` e emitir o modelo; venda sem config conclui com aviso; falha não-transitória gera cupom pendente/erro. DEFERIDO — `apps/pdv/frontend` sem código; PDV real é Flutter/legado sem integração fiscal (outra stack). Precisa decisão de qual app é o alvo. | feature própria (Dart/Flutter) | PENDENTE |
| B8 | erp-api `catalog` — rotas de `price-list`/`technical-sheet` (e similares) sem `@RequirePermission` | ~10 rotas de catálogo herdam o gap de autorização fina: `PermissionGuard` libera quando `required` é `undefined`, então qualquer membro autenticado da org lê/escreve. Achado security+typescript review (HIGH, **pré-existente**). As 3 rotas de `fiscal-parameters` que a 014 toca **já foram corrigidas** (`store.catalog.manage`/`org.view`); o resto fica para um /bugfix próprio. | /bugfix próprio | PENDENTE |
| B9 | erp-api `pos-fiscal-settings` repo (`save`) | `upsert({ where: { id } })` chaveado pelo `id` gerado em memória, não por `organizationId` (@unique) — mesma classe de corrida do achado da 014 (P2002 na 1ª gravação concorrente). Na 014 a `PosFiscalSettings` foi **registrada em `TENANT_SCOPED_MODELS`** (defesa em profundidade que faltava desde a 013), mas o upsert-por-`id` continua. Corrigir para chavear por `organizationId`. Achado typescript review (HIGH, **pré-existente da 013**). | /bugfix próprio | PENDENTE |
| B7-emissão | erp-api (create-pos-sale/issue-nfe) + PDV | **Ligar a emissão real ao resolvedor** da 015: quem monta o pedido chama `ResolveItemPisCofinsUseCase` (lê `ProductFiscal.pisCofinsGroupId`), popula `NfeItemInput.pis`/`cofins` e envia à fiscal-api. Hoje o resolvedor existe e é testado, mas **nenhum caller o usa** — toda NF-e ainda cai no fallback CST 01/49. É o B7 (disparo PDV→fiscal-api, Flutter/legado) somado ao wiring no erp-api. Achado typescript review (HIGH #2, deferido por decisão — plan D5). | feature própria (B7) | PENDENTE |
| B10 | services/fiscal-api `nfe-item` validator (zod) | Quando B7-emissão ligar `NfeItemInput.pis`/`cofins` ao contrato HTTP, a **fiscal-api deve revalidar** CST (conjunto suportado) e alíquota (0–100) no seu próprio validador — hoje `buildContributionXml` confia no caller (a fiscal-api "não conhece grupos"). Dormant até B7 (o DTO público `NfeItemDto` ainda não tem `pis`/`cofins`). Achado security review (MEDIUM). Tipo `PisCofinsCst` (união literal) já reduz o risco no builder. | /bugfix com B7 | PENDENTE |
| B11 | erp-web `fiscal-issqn-group` + `fiscal-icms-group` edit pages | Form de edição sem `key={group.id}` no `FiscalGroupFormView` — navegar entre dois grupos de edição reaproveita a instância e mantém a baseline "salva" do grupo anterior (salvar poderia comitar valores errados). **A 019 já nasce com `key`** (achado react-review aplicado no `ipi-group-edit-page`); os dois irmãos têm o mesmo gap pré-existente. Correção trivial (add `key`). Achado react review (HIGH, **pré-existente 016/018**). | /bugfix próprio | PENDENTE |

Status possíveis: NÃO INICIADA · EM ANDAMENTO · AGUARDANDO CONFERÊNCIA · CONCLUÍDA · BLOQUEADA.
