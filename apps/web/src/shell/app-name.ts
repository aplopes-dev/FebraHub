/** Nome exibido na marca da casca — os dois presets usam o mesmo. */
export const APP_NAME = "FebraHub";

/** Nome na tela de acesso — o mesmo produto, antes de entrar. */
export const AUTH_BRAND_NAME = "FebraHub";
