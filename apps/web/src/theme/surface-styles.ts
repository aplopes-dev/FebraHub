/**
 * Multiplicador MUI `sx` para `theme.shape.borderRadius`
 * (`1` = raio do tema, hoje 8px).
 *
 * Existe para cards e painéis de tela usarem o mesmo raio dos componentes do
 * DS sem repetir o número.
 */
export const surfaceBorderRadius = 1;
